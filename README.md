# My Advent of Code 2024 solutions, with PyTorch

## Requirements

* Python 3.11
* uv

## Run solutions

`uv run python aoc/day1.py`

Where you can replace the `day1` with the given day you want to run

## Formatting

`uv run ruff format`
